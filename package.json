{
  "name": "unicalc",
  "description": "Simple weighted calculator, useful when working out your university classification",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "gulp-browserify": "~0.4.6",
    "gulp": "~3.5.2",
    "mocha-phantomjs": "~3.3.2",
    "chai": "~1.9.1"
  },
  "scripts": {
    "test": "gulp; mocha-phantomjs -R dot -s localToRemoteUrlAccessEnabled=true -s webSecurityEnabled=false test/test.runner.html"
  }
}
